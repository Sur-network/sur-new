# مشخصات فنی سرویس آف‌چین Verifier

> این سند رفتار دقیق سرویس آف‌چین `verifier` را توصیف می‌کند — همان سرویسی که کلید on-chain `verifier` (در `ValidatorsRegistry`) را در اختیار دارد و مسئول احراز موبایل/تلگرام، ثبت liveness ولیدیتورها، و اجرای تصمیمات هیأت‌مدیره‌ی ولیدیتورها درباره‌ی چرخش این کلید است. برای منطق on-chain مرتبط به `sur-smart-contracts-reference.md` و برای تصمیمات معماری به `sur-blockchain-design-doc.md` مراجعه کن.

---

## ۱. نقش کلی — این سرویس دقیقاً چه مشکلی را حل می‌کند

سه وظیفه‌ی کاملاً متفاوت دارد که به یک کلید امضاکننده‌ی مشترک (`verifier`) گره خورده‌اند:

1. **احراز هویت** — تأیید این‌که شماره‌موبایل/آیدی تلگرام اعلام‌شده‌ی یک ولیدیتور واقعاً متعلق به خودش است.
2. **گزارش liveness** — تأیید این‌که یک ولیدیتور (در `Probation` یا `Active`) واقعاً یک نود سالم دارد، نه فقط یک کیف‌پول که می‌تواند امضا کند.
3. **نگه‌داری داده‌ی حساس آف‌چین** — شماره‌موبایل، آیدی تلگرام، و آدرس/`nodeID` نودها، که عمداً هرگز روی زنجیره‌ی عمومی ثبت نمی‌شوند.
4. **✅ تصمیم تازه — احراز مداوم محل جغرافیایی نود:** طبق شرط تازه‌ی عضویت (بخش ۶ سند طراحی بلاک‌چین («شرط تازه — الزام حضور فیزیکی نود»))، نود یک ولیدیتور باید **همیشه، بدون استثنا**، از لحاظ فیزیکی داخل مرزهای جغرافیایی ایران باشد. این سرویس، در همان چرخه‌ی liveness (نه یک چرخه‌ی جدا)، این شرط را به‌طور مداوم احراز می‌کند — جزئیات کامل در بخش ۶.۵ پایین.

---

## ۲. ورودی‌ها

### ۲.۱ ورودی از کاربر (از طریق اپلیکیشن همراه)
| فیلد | نوع | اجباری؟ |
|---|---|---|
| آدرس اتریومی ولیدیتور | `address` | بله (کلید اصلی هر رکورد) |
| شماره موبایل | رشته | بله (برای مسیر وریفای موبایل) |
| آیدی تلگرام | رشته | بله (برای مسیر وریفای تلگرام) |
| `nodeID` (بخش کلید عمومی enode، **نه کل enode**) | رشته‌ی هگزادسیمال ۱۲۸ کاراکتری | بله (برای liveness) |
| کد تأیید پیامکی/تلگرامی (هنگام فرآیند وریفای) | رشته‌ی کوتاه | بله، فقط در لحظه‌ی وریفای |

🔶 نام و نوع شخصیت (`Individual`/`Legal`) لازم نیست از این اپ عبور کند — کاربر می‌تواند مستقیم `IdentityRegistry.registerIdentity` (⚠️ اصلاحیه: نه دیگر `ValidatorsRegistry`، هویت به قرارداد مستقلی منتقل شد — `sur-identity-registry-spec.md`) را خودش صدا بزند؛ ولی اگر بخواهید تجربه‌ی کاربری یکپارچه بسازید، این اپ می‌تواند یک دکمه/فرم برای ارسال همان تراکنش هم داشته باشد.

### ۲.۲ ورودی از شبکه‌ی P2P
- خروجی دوره‌ای `admin_peers` از نود مانیتورینگ خودِ سرویس — شامل `id` (nodeID) و `network.remoteAddress` (IP:پورت فعلی) هر peer متصل.
- ✅ **تصمیم تازه:** همین `remoteAddress` (که از قبل جمع‌آوری می‌شد) اکنون منبع ورودی احراز جغرافیایی هم هست — نیازی به هیچ داده‌ی تازه‌ای از کاربر یا از خودِ نود نیست؛ فقط یک پردازش تازه (IP-geolocation) روی همین IP که همین الان داریم.

### ۲.۴ ✅ ورودی تازه — پایگاه‌داده/سرویس IP-geolocation
| داده | منبع | هدف |
|---|---|---|
| کد کشور متناظر با یک IP | یک سرویس/پایگاه‌داده‌ی IP-geolocation (مثلاً MaxMind GeoLite2 آفلاین، یا یک API تجاری) | تشخیص این‌که آیا `remoteAddress` هر peer از داخل ایران است یا نه |

🔶 **سؤال باز:** انتخاب دقیق سرویس/پایگاه‌داده (آفلاین رایگان در برابر API تجاری با دقت بالاتر) و دوره‌ی به‌روزرسانی‌اش هنوز مشخص نشده — به بخش ۹ (سؤالات باز) مراجعه کن.

### ۲.۳ ورودی از زنجیره (RPC)
| داده | متد RPC | هدف |
|---|---|---|
| لیست ولیدیتورهای فعال | `ValidatorsRegistry.getValidators()` | فهرست کسانی که باید چک تولید بلاک بشوند |
| وضعیت هر ولیدیتور | `ValidatorsRegistry.getValidatorInfo(address)` | تشخیص `Probation`/`Active`/`Demoted` برای انتخاب روش چک |
| `miner`/`coinbase` هر بلاک اخیر | `eth_getBlockByNumber` (حلقه روی بلاک‌های اخیر) | چک واقعی تولید بلاک برای ولیدیتورهای `Active` |
| ارتفاع فعلی زنجیره | `eth_blockNumber` | مبنای مقایسه برای چک sync-بودن نود کاندیدای `Probation` |

---

## ۳. خروجی‌ها

### ۳.۱ تراکنش‌های on-chain (امضاشده با کلید `verifier`)
| تابع | چه زمانی صدا زده می‌شود |
|---|---|
| `ValidatorsRegistry.reportLiveness(address, bool)` | هر دوره‌ی polling (✅ **تصمیم قطعی: هر ۱۰ دقیقه** — نه بازه‌ی قبلی ۱۰-۱۵؛ باید دقیقاً با `MIN_LIVENESS_CHECK_INTERVAL` قرارداد هماهنگ باشه)، برای هر ولیدیتور تحت پایش |
| `ValidatorsRegistry.setPhoneVerified(address, bool)` | بلافاصله بعد از موفقیت/شکست فرآیند وریفای پیامکی |
| `ValidatorsRegistry.setTelegramVerified(address, bool)` | بلافاصله بعد از موفقیت/شکست فرآیند وریفای تلگرامی |

### ۳.۲ اعلان‌ها (به کاربر، از طریق همان اپ)
- هشدار پیشگیرانه وقتی `nodeID` ثبت‌شده‌ی یک ولیدیتور دیگر در `admin_peers` دیده نمی‌شود (قبل از رسیدن به `inactivityThreshold` — بخش «راه‌حل تازه» در سند طراحی را ببین).

### ۳.۳ لاگ داخلی
هر polling، هر تصمیم، و هر تراکنش، با timestamp کامل لاگ می‌شود — برای حسابرسی بعدی و دیباگ اختلاف‌نظر احتمالی («چرا فلان ولیدیتور دموت شد؟»).

---

## ۴. معماری اجرا

```
+------------------------------------------------+  
|           Verifier Service (Node.js)           |  
|                                                |  
| +-----------+   +--------------+   +---------+ |  
| | Scheduler |-->|  Liveness &  |-->|Tx Signer| |  
| |(10-15 min)|   |Identity Logic|   |         | |  
| +-----------+   +--------------+   +---------+ |  
|                         |               |      |  
|                     +------+            |      |  
|                     |SQLite|            |      |  
|                     +------+            |      |  
+------------------------------------------------+  
                          |               |         
                +------------------+ +-------------+
                | Monitoring Node  | |  RPC Node   |
                |(Besu, static peer| |(chain reads)|
                |to all validators)| |             |
                +------------------+ +-------------+
```

### اجزا
1. **یک نود Besu اختصاصی برای مانیتورینگ** — به‌عنوان `static peer` به تمام ولیدیتورها وصل می‌شود (نه صرفاً از discovery معمولی) تا `admin_peers` همیشه کامل باشد. نیازی نیست full validator باشد؛ فقط باید peer معتبر شبکه باشد.
2. **دسترسی RPC به یک full node** — می‌تواند همان نود مانیتورینگ باشد یا یک نود جدا؛ برای خواندن state قراردادها و بلاک‌های اخیر.
3. **یک کلید امضاکننده (`verifier` hot wallet)** — باید همیشه موجودی سورن کافی برای گس داشته باشد (بخش «پیامد اقتصادی» در سند طراحی).
4. **پایگاه‌داده** — `SQLite` (همان الگوی سرویس RewardRouter در سند طراحی؛ کافی برای یک سرویس تک‌پردازشی با اجرای زمان‌بندی‌شده).
5. **Scheduler** — یک حلقه/cron داخلی، نه یک سرویس وب عمومی (این سرویس API عمومی ندارد؛ فقط با اپ همراه و با زنجیره حرف می‌زند).

### ✅ افزونگی (تصمیم تازه — مشروح)
یک نمونه‌ی تکی کافی نیست. با توجه به این‌که خرابی این سرویس، ظرف دقیقاً ۱ ساعت باعث دموت/اسلش نادرست **همه‌ی** ولیدیتورها می‌شود (نه یک خطای بی‌ضرر)، حداقل **۲ تا ۳ نمونه‌ی مستقل** (سرورهای جدا، ترجیحاً دیتاسنتر/ارائه‌دهنده‌ی جدا) پیشنهاد می‌شود.

**معماری پیشنهادی — تفکیک «چک‌کردن» از «امضاکردن»:**
این دو نگرانی جنس متفاوتی دارند و بهتر است جدا طراحی شوند:
- **چک‌کردن (polling، خواندن `admin_peers`/بلاک‌ها، تصمیم `isLive`):** ارزان و بی‌خطر برای تکرار — می‌توان ۲ تا ۳ نمونه‌ی کاملاً مستقل از این منطق را هم‌زمان اجرا کرد، بدون هیچ هماهنگی بین‌شان. چون فراخوانی دوباره‌ی `reportLiveness` با همان مقدار کاملاً بی‌ضرر است (فقط timestamp را به‌روزرسانی می‌کند)، تداخل یا رقابت بین نمونه‌ها مشکلی ایجاد نمی‌کند.
- **امضاکردن (نگه‌داری کلید `verifier` و ارسال واقعی تراکنش):** این بخش باید **متمرکز و امن** بماند — نه یک فایل کلید خام کپی‌شده روی هر سرور. پیشنهاد: یک سرویس امضای کوچک و جدا (remote signer)، که خودش می‌تواند با تمرکز کمتر روی گنجایش و بیشتر روی در‌دسترس‌بودن (HA) طراحی شود (مثلاً پشت یک load balancer با چند نمونه‌ی خودش که به یک HSM/KMS مشترک متصل‌اند)، که هر سه نمونه‌ی polling بالا فقط نتیجه‌ی `isLive` محاسبه‌شده‌شان را برایش می‌فرستند و او تراکنش را امضا و ارسال می‌کند.

این تفکیک باعث می‌شود افزونگی در بخش پرهزینه‌تر (polling/چک‌کردن نودها) ساده و ارزان باشد، در حالی که سطح حمله‌ی کلید خصوصی (بخش حساس امنیتی) کوچک و متمرکز می‌ماند.

اگر این افزونگی هم کافی نبود، بازطراحی قرارداد برای پشتیبانی از چند آدرس `verifier` هم‌زمان یک گزینه‌ی آینده است (نیازمند تغییر کد `ValidatorsRegistry`).

### زبان و پشته‌ی فنی
- **Node.js / TypeScript** — برای هماهنگی با سرویس RewardRouter که قبلاً با همین پشته طراحی شد (استفاده از `better-sqlite3`).
- کتابخانه‌ی تعامل با زنجیره: ✅ **اصلاحیه: `viem`** (نه `ethers.js` — این دو کاملاً هم‌ارز بودند، ولی چون `viem` از قبل برای RewardRouter انتخاب شده بود، برای یکسان‌سازی و کاهش هزینه‌ی نگه‌داری، همین‌جا هم به `viem` تغییر کرد. تفاوت اولیه صرفاً یک ناهماهنگی سهوی بین دو سند بود، نه یک تصمیم طراحی).
- تعامل با `admin_peers`: یک فراخوانی JSON-RPC خام (`admin_peers`) روی همان نود مانیتورینگ — نیازی به کتابخانه‌ی خاص نیست.
- ارسال پیامک/پیام تلگرام برای فرآیند وریفای: 🔶 وابسته به سرویس‌دهنده‌ی انتخابی (باز).

---

## ۵. داده‌های ذخیره‌شده و طرح جداول

⚠️ **اصل حاکم: هیچ داده‌ی خام حساسی (شماره‌موبایل خام، آیدی تلگرام خام، IP خام) هرگز روی زنجیره نمی‌رود — فقط همین‌جا.**

### جدول `validators`
| ستون | نوع | توضیح |
|---|---|---|
| `eth_address` | TEXT PRIMARY KEY | آدرس اتریومی ولیدیتور |
| `node_id` | TEXT | بخش کلید عمومی enode (ثابت، نه IP) |
| `phone_raw` | TEXT | شماره موبایل خام (رمزنگاری‌شده در حالت سکون توصیه می‌شود) |
| `telegram_raw` | TEXT | آیدی تلگرام خام |
| `phone_verified_onchain` | BOOLEAN | آخرین وضعیت ارسال‌شده به زنجیره (کش محلی) |
| `telegram_verified_onchain` | BOOLEAN | همان‌طور |
| `registered_at` | TIMESTAMP | |
| `updated_at` | TIMESTAMP | |
| `exited_at` | TIMESTAMP NULL | ✅ **تازه.** پر می‌شود وقتی `ValidatorsRegistry.withdrawStake()` این آدرس را کامل می‌بیند (یعنی چرخه‌ی خروج تمام شده)؛ مبنای شمارش سیاست نگه‌داری زیر است |

### جدول `liveness_log`
| ستون | نوع | توضیح |
|---|---|---|
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT | |
| `eth_address` | TEXT | |
| `checked_at` | TIMESTAMP | |
| `method` | TEXT | `'node_sync'` یا `'block_production'` |
| `is_live` | BOOLEAN | نتیجه‌ی چک |
| `detail` | TEXT | جزئیات خام (مثلاً ارتفاع بلاک مشاهده‌شده، یا تعداد بلاک تولیدشده) |
| `tx_hash` | TEXT NULL | هش تراکنش `reportLiveness` (اگر ارسال شد) |

### جدول `node_discovery_cache`
| ستون | نوع | توضیح |
|---|---|---|
| `node_id` | TEXT PRIMARY KEY | |
| `last_seen_ip` | TEXT | آخرین IP دیده‌شده در `admin_peers` |
| `last_seen_at` | TIMESTAMP | |
| `country_code` | TEXT | ✅ تازه — نتیجه‌ی IP-geolocation روی `last_seen_ip` (مثلاً `IR`) |
| `is_iran` | BOOLEAN | ✅ تازه — `country_code == 'IR'`؛ همین فیلد مستقیم در گام ۶ جریان کاری (بخش ۶) برای اجباری‌کردن `isLive=false` استفاده می‌شود |

### ✅ سیاست نگه‌داری و حذف داده‌ی خام (تصمیم تازه، مشروح)

**قاعده‌ی کلی:** داده‌ی حساس خام (`phone_raw`, `telegram_raw`, `node_id`) تا **۱ سال پس از خروج** ولیدیتور از شبکه نگه‌داری می‌شود، سپس حذف می‌شود.

**چرا مبنای شمارش «خروج» است، نه «ثبت‌نام»:** اگر شمارش از تاریخ ثبت‌نام باشد، داده‌ی یک ولیدیتوری که سال‌هاست فعال است وسط راه پاک می‌شود — که غلط است، چون تا وقتی فعال است همچنان باید verify شود. پس در واقع دو حالت داریم:
1. **تا وقتی ولیدیتور فعال است (`exited_at IS NULL`):** نگه‌داری بدون هیچ محدودیت زمانی ادامه دارد — چون سرویس هنوز به این داده برای liveness/احراز نیاز دارد.
2. **از لحظه‌ای که `exited_at` پر می‌شود (خروج کامل، یعنی `withdrawStake()` موفق روی زنجیره دیده شود):** یک شمارش‌معکوس ۱ساله شروع می‌شود. یک job دوره‌ای (مثلاً روزانه) رکوردهایی با `exited_at` بیش از ۳۶۵ روز قبل را پیدا می‌کند و فیلدهای `phone_raw`، `telegram_raw`، `node_id` را (نه کل ردیف را) پاک/بی‌نام می‌کند.

**تفکیک مهم — چه چیزی حذف می‌شود، چه چیزی نه:**
- فیلدهای **حساس شخصی** (`phone_raw`, `telegram_raw`, `node_id`) در جدول `validators` بعد از این بازه پاک می‌شوند.
- رکوردهای جدول `liveness_log` **حذف نمی‌شوند** — چون این جدول از ابتدا هیچ داده‌ی شخصی‌ای ندارد (فقط آدرس اتریومی، timestamp، و نتیجه‌ی بولی)، و آدرس اتریومی به‌هرحال از قبل و برای همیشه روی خودِ زنجیره‌ی عمومی قابل‌مشاهده است — حذفش از دیتابیس داخلی ما هیچ حریم خصوصی‌ای اضافه نمی‌کند، فقط تاریخچه‌ی عملیاتی/حسابرسی را از بین می‌برد که ارزش نگه‌داری دارد.

🔶 این یک تصمیم عملیاتیِ منطقی است، نه یک نظر حقوقی قطعی — پیشنهاد می‌شود همین سیاست در متن Validator Agreement/سپیدنامه هم صریح نوشته شود و نهایتاً توسط مشاور حقوقی تأیید شود.

---

## ۶. جریان کاری — هر دوره‌ی polling (✅ تصمیم قطعی: هر ۱۰ دقیقه)

1. از `ValidatorsRegistry.getValidators()` + جدول داخلی، فهرست کامل ولیدیتورهای تحت پایش را بساز (`Probation` + `Active` + `Demoted` در حال بازگشت).
2. `admin_peers` را از نود مانیتورینگ بخوان؛ جدول `node_discovery_cache` را به‌روزرسانی کن.
3. ✅ **گام تازه — احراز جغرافیایی، پیش‌نیاز هر liveness مثبت:** برای هر peer در `admin_peers`، `remoteAddress` را از طریق پایگاه‌داده/سرویس IP-geolocation (بخش ۲.۴) چک کن. نتیجه (کد کشور، `is_iran: bool`) را در `node_discovery_cache` ذخیره کن. جزئیات کامل قانون و پیامدش در بخش ۶.۵ زیر.
4. برای هر ولیدیتور `Probation`/`Demoted`: ✅ **الگوریتم دقیق (تصمیم تازه، رفع ابهام قبلی):** چک sync-بودن **فقط از طریق حضور در `admin_peers`** انجام می‌شود — یعنی آیا `node_id`ش الان به‌عنوان یک peer متصل به نود مانیتورینگ دیده می‌شود یا نه. **عمداً به RPC خودِ نود کاندیدا وصل نمی‌شویم** — نه لازم است کاندیدا پورت RPC‌اش را عمومی کند (ریسک امنیتی برای خودش)، نه ما نیاز به آن داریم؛ صرف حضور به‌عنوان یک peer سالم و متصل، برای این مرحله (که فقط یک گیت اولیه‌ی «آیا اصلاً یک نود واقعی داری» است، نه اثبات کامل صحت عملکرد) کافی است. تست واقعی و دقیق‌تر وقتی اتفاق می‌افتد که به `Active` ترفیع پیدا کند و چک تولید بلاک واقعی روی او اجرا شود (قدم بعدی).
5. برای هر ولیدیتور `Active`: ✅ **الگوریتم دقیق (تصمیم تازه):** پنجره = آخرین بلاک‌هایی که `timestamp`شان در `inactivityThreshold` ثانیه‌ی اخیر است (با `blockperiodseconds=3` و `inactivityThreshold=1 ساعت`، یعنی حدود ۱۲۰۰ بلاک). اگر فیلد `miner` **حتی یک بلاک** در این پنجره برابر آدرس این ولیدیتور بود → `isLive=true`؛ صفر بلاک → `isLive=false`. (با رأی‌دهی round-robin بین N ولیدیتور فعال، هر ولیدیتور به‌طور میانگین حدود `۱۲۰۰/N` نوبت در این پنجره دارد — حتی با N=۱۰۰، این یعنی ~۱۲ نوبت، پس آستانه‌ی «حداقل یک بلاک» عمداً سخت‌گیرانه نیست، فقط غیبت مطلق را می‌گیرد.)
6. ✅ **ترکیب نهایی isLive با نتیجه‌ی گام ۳:** اگر `is_iran == false` برای `node_id` یک ولیدیتور، `isLive` برای آن ولیدیتور **به‌طور اجباری `false` گزارش می‌شود** — صرف‌نظر از این‌که چک sync/تولید بلاک (گام ۴/۵) چه نتیجه‌ای داده باشد. یعنی یک نود می‌تواند کاملاً سالم و درحال‌تولید-بلاک باشد و بازهم `isLive=false` بگیرد، فقط به‌خاطر این‌که خارج از ایران دیده شده.
7. برای هرکدام، `reportLiveness(address, isLive)` را روی زنجیره بزن؛ نتیجه (و کد کشور تشخیص‌داده‌شده) را در `liveness_log` ثبت کن.
8. اگر یک `node_id` ثبت‌شده در `admin_peers` پیدا نشد، یک اعلان هشدار برای همان ولیدیتور صف کن (بخش ۳.۲). ✅ اگر `is_iran == false` تشخیص داده شد، یک اعلان هشدار **فوری و جداگانه** (نه فقط هشدار غیبت معمولی) برای همان ولیدیتور و برای هیأت‌مدیره‌ی ولیدیتورها صف کن.

### ۶.۵ ✅ تصمیم تازه — شرط الزام حضور فیزیکی نود در ایران

طبق یک تصمیم تازه‌ی پروژه (بخش ۶ سند طراحی بلاک‌چین («شرط تازه — الزام حضور فیزیکی نود»)، بخش ۶.۷ سند توکنومیکس)، نود یک ولیدیتور باید **همیشه و بدون استثنا** از لحاظ فیزیکی داخل مرزهای جغرافیایی ایران باشد. این سرویس این شرط را در **همان چرخه‌ی liveness موجود** (نه یک مکانیزم جدا) احراز می‌کند — دلیل ادغام: زیرساخت لازم (خواندن دوره‌ای `remoteAddress` از `admin_peers`) از قبل برای liveness وجود دارد؛ ساختن یک چرخه‌ی جدا فقط برای همین یک چک، تکرار بی‌دلیل زیرساخت است.

**مکانیزم دقیق:**
- منبع IP: همان `network.remoteAddress` که در `admin_peers` برای liveness هم استفاده می‌شود — نه یک ورودی جدا از کاربر یا نود.
- تشخیص کشور: از طریق پایگاه‌داده/سرویس IP-geolocation (بخش ۲.۴).
- پیامد: طبق گام ۶ بالا، هر تشخیص `is_iran == false` باعث `isLive=false` اجباری می‌شود — یعنی این شرط از طریق **همان مسیر پیامدی liveness موجود** (غیبت پیوسته‌ی گزارش مثبت → دموت بعد از `inactivityThreshold`) اثر می‌گذارد، نه یک مسیر پیامدی جدا.

⚠️ **محدودیت صادقانه‌ای که باید صریح گفته شود:** چون پیامد از طریق مسیر liveness موجود اعمال می‌شود، یک نود که به‌تازگی از ایران خارج شده، تا رسیدن به `inactivityThreshold` (فعلاً ۱ ساعت — 🔶 هنوز نهایی نشده) **دموت نمی‌شود**، نه فوری. اگر هدف پروژه صفر-تحمل و فوری‌بودن پیامد است (نه فقط «مثل هر غیبت دیگه‌ای رفتار کن»)، باید یک مسیر پیامدی جدا و سریع‌تر (مثلاً یک تابع جداگانه در `ValidatorsRegistry.sol` مثل `reportGeoViolation()` که فوری و بدون‌انتظار دموت می‌کند) طراحی شود — این یک 🔶 سؤال باز و تصمیم فنی جداست که هنوز گرفته نشده (به بخش ۹ مراجعه کن).

**دقت و محدودیت IP-geolocation:** تشخیص کشور از روی IP کامل و بی‌خطا نیست — VPN/پروکسی می‌تواند IP را جعل کند. این سرویس فقط می‌تواند «IP گزارش‌شده از کجاست» را احراز کند، نه «کجای فیزیکی واقعی سرور». اجرای این شرط تا حد زیادی متکی به این است که ولیدیتورها آگاهانه همکاری کنند (مثلاً امضای یک تعهدنامه‌ی خارج از زنجیره در کنار این چک فنی) — این محدودیت باید صریحاً به ولیدیتورها اعلام شود.



---

## ۷. امنیت

- کلید خصوصی `verifier` باید در یک secret manager (نه فایل متنی ساده) نگه‌داری شود.
- شماره‌موبایل/آیدی تلگرام/`node_id` در دیتابیس باید در حالت سکون رمزنگاری شوند؛ دسترسی به دیتابیس باید محدود باشد.
- این سرویس یک **نقطه‌ی تمرکز اعتماد واقعی** است — اگر از کار بیفتد، هیچ ولیدیتوری نمی‌تواند از `Probation` عبور کند و هیچ گزارش liveness تازه‌ای ثبت نمی‌شود (که در عمل یعنی بعد از `inactivityThreshold`، همه به‌غلط `Demoted` می‌شوند).

### ✅ مانیتورینگ سلامت خودِ سرویس (تصمیم/پیشنهاد تازه — مشروح)
- **Dead-man's-switch**: هر نمونه از سرویس هر ۵ دقیقه یک پینگ «زنده‌ام» به یک ابزار مانیتورینگ بیرونی (مثل Healthchecks.io) می‌فرستد.
- **آستانه‌ی هشدار: ۲۰ تا ۳۰ دقیقه** بدون پینگ — عمداً خیلی زودتر از سقف واقعی ۱ ساعته‌ی `inactivityThreshold`، تا فرصت واقعی برای واکنش انسانی قبل از دموت/اسلش نادرست باقی بماند.
- **کانال هشدار باید پیجینگ واقعی باشد** (تماس/پیامک به فرد/تیم مسئول)، نه فقط ایمیل — با توجه به شدت و برگشت‌ناپذیری نسبی عواقب (اسلش).
- **چه کسی پیجر می‌شود — پیشنهاد مشخص:** یک چرخه‌ی on-call بین چند نفر از کمیته‌ی فنی بنیاد یا اعضای هیأت‌مدیره‌ی ولیدیتورها (چون این‌ها افرادی هستند که از قبل با زیرساخت شبکه درگیرند). **قاعده‌ی escalation:** اگر نفر اول ظرف ۱۰ دقیقه پاسخ ندهد، هشدار خودکار به نفر دوم چرخه ارسال شود؛ اگر تا ۲۰ دقیقه هیچ‌کدام پاسخ ندادند، به کل چرخه هم‌زمان ارسال شود. مشخص‌کردن اسامی دقیق افراد این چرخه هنوز یک تصمیم عملیاتی جداست (بخش ۹، سؤال باقی‌مانده).
- **لایه‌ی شفافیت اضافه:** چون تراکنش‌های `reportLiveness` خودشان on-chain و timestamp‌دار هستند، یک دیده‌بان/بات عمومی و مستقل (مثلاً یک صفحه‌ی وب ساده‌ی «آخرین گزارش موفق verifier: X دقیقه پیش») می‌تواند این وضعیت را به‌طور عمومی و بدون وابستگی به مانیتورینگ داخلی ما نمایش دهد — هم شفافیت برای جامعه، هم یک لایه‌ی هشدار مستقل و غیررسمی (هرکسی، از جمله خودِ ولیدیتورها، می‌تواند متوجه توقف سرویس بشود، حتی اگر مانیتورینگ داخلی ما به هر دلیلی هم خودش خراب شده باشد).

---

## ۸. پیوست پیاده‌سازی (تصمیمات تازه — برای کفایت جهت واگذاری به یک ابزار کدنویسی)

### ۸.۱ رابط API بین اپ همراه و سرویس

احراز هویت با **امضای اتریومی** (الگوی Sign-In-with-Ethereum)، نه رمز عبور — چون کاربر از قبل یک کیف‌پول دارد:

```
GET  /api/v1/challenge?ethAddress=0x...
     → { message: "Sur Verifier — Sign to authenticate — nonce: <random> — expires: <ts>" }

POST /api/v1/register
     body: { ethAddress, signature, nodeId, phone, telegramId }
     → ثبت/به‌روزرسانی رکورد در جدول validators

POST /api/v1/phone/request-otp
     body: { ethAddress, signature }
     → ارسال کد به شماره‌ی ثبت‌شده

POST /api/v1/phone/verify-otp
     body: { ethAddress, signature, code }
     → در صورت تطابق: setPhoneVerified(ethAddress, true) روی زنجیره

POST /api/v1/telegram/request-otp   (مشابه بالا)
POST /api/v1/telegram/verify-otp    (مشابه بالا)

PUT  /api/v1/node-id
     body: { ethAddress, signature, nodeId }
     → به‌روزرسانی nodeId (مثلاً بعد از migration/نصب مجدد نود)

GET  /api/v1/status/:ethAddress   (بدون نیاز به امضا — فقط خواندنی و غیرحساس)
     → { phoneVerified, telegramVerified, lastLivenessConfirmation, isCurrentlyLive }
```

هر امضا باید یک `message` تازه از `/challenge` (با `expires` کوتاه، مثلاً ۵ دقیقه) امضا کند — جلوگیری از replay.

### ۸.۲ مدیریت خطا و جلوگیری از تراکنش‌های تکراری بین نمونه‌ها

- هر نمونه‌ی polling (بخش ۴، افزونگی) مستقل کار می‌کند و نتیجه‌ی `isLive` محاسبه‌شده‌اش را به سرویس امضای مشترک می‌فرستد؛ **دیدوپلیکیشن در همان سرویس امضای مشترک انجام می‌شود** (نه بین نمونه‌های polling): اگر برای یک آدرس مشخص با همان نتیجه ظرف ۵ دقیقه‌ی اخیر تراکنشی ارسال شده، درخواست تازه نادیده گرفته می‌شود — این هم هزینه‌ی گس اضافی را کنترل می‌کند، هم چون فقط **یک** سرویس امضا nonce را مدیریت می‌کند، هیچ تداخل nonce‌ای بین چند «کیف‌پول» پیش نمی‌آید.
- خطای RPC (نود مانیتورینگ یا نود RPC در دسترس نیست): تا ۳ بار با backoff نمایی (۲، ۴، ۸ ثانیه) تلاش مجدد؛ اگر باز هم ناموفق بود، همان دور polling را رد کن (نه crash کردن کل سرویس) و در لاگ ثبت کن.
- خطای ارتباط با سرویس امضا: نتیجه به‌سادگی رها می‌شود (نیازی به صف نیست، چون دور polling بعدی به‌زودی دوباره محاسبه می‌کند)؛ اگر این وضعیت ادامه‌دار شود، dead-man's-switch (بخش ۷) به‌طور طبیعی آن را می‌گیرد چون هیچ گزارشی موفق نمی‌شود.

### ۸.۳ زیرساخت — HashiCorp Vault + Docker Compose (تصمیم تازه)

**نگه‌داری کلید:** کلید خصوصی `verifier` در **HashiCorp Vault** (Transit Secrets Engine، برای امضای remote بدون خروج کلید خام از Vault) نگه‌داری می‌شود؛ سرویس امضا با Vault API صحبت می‌کند، نه با یک فایل کلید محلی.

**اسکلت `docker-compose.yml` (سطح مفهومی، نه نهایی):**
```yaml
services:
  vault:
    image: hashicorp/vault:latest
    # ... تنظیمات unseal/policy جدا مستند شود

  besu-monitor:
    image: hyperledger/besu:latest
    # static-nodes.json شامل enode همه‌ی ولیدیتورها
    volumes:
      - ./besu-data:/data

  verifier-signer:
    build: ./signer
    depends_on: [vault]
    environment:
      - VAULT_ADDR
      - VAULT_TOKEN
      - RPC_URL
    # تنها جزئی که به Vault دسترسی دارد

  verifier-poller-1:
    build: ./poller
    depends_on: [besu-monitor, verifier-signer]
    environment:
      - MONITOR_NODE_RPC
      - CHAIN_RPC_URL
      - SIGNER_SERVICE_URL
      - DB_PATH=/data/verifier.sqlite
    volumes:
      - ./data:/data

  verifier-poller-2:
    # نمونه‌ی دوم، همان تنظیمات، برای افزونگی (بخش ۴)

  api-gateway:
    build: ./api
    depends_on: [verifier-poller-1]
    ports:
      - "443:443"
    environment:
      - SMS_PROVIDER_API_KEY   # 🔶 هنوز باز
      - TELEGRAM_BOT_TOKEN     # 🔶 هنوز باز
```

**فهرست env varهای اصلی:**
| متغیر | توضیح |
|---|---|
| `VAULT_ADDR`, `VAULT_TOKEN` | دسترسی به HashiCorp Vault |
| `CHAIN_RPC_URL` | آدرس RPC نود برای خواندن state/بلاک |
| `MONITOR_NODE_RPC` | آدرس RPC نود مانیتورینگ (برای `admin_peers`) |
| `DB_PATH` | مسیر فایل SQLite |
| `HEALTHCHECK_URL` | آدرس ابزار dead-man's-switch |
| `SMS_PROVIDER_API_KEY`, `TELEGRAM_BOT_TOKEN` | 🔶 هنوز باز (سؤال باقی‌مانده #۱) |

---

## ۹. سؤالات باز

جواب کامل و مشروح موارد قبلاً حل‌شده در متن اصلی سند آمده (افزونگی: بخش ۴؛ سیاست نگه‌داری داده: بخش ۵؛ مانیتورینگ سلامت: بخش ۷؛ API/الگوریتم/زیرساخت: بخش ۸). فقط موارد واقعاً باقی‌مانده:

| # | مورد |
|---|---|
| ۱ | سرویس‌دهنده‌ی پیامک/تلگرام برای فرآیند وریفای کدام است؟ |
| ۲ | اسامی دقیق افراد چرخه‌ی on-call برای پیجینگ (بخش ۷) |
| ۳ | ✅ **تازه:** انتخاب دقیق سرویس/پایگاه‌داده‌ی IP-geolocation (آفلاین رایگان مثل MaxMind GeoLite2 در برابر API تجاری با دقت بالاتر) و دوره‌ی به‌روزرسانی‌اش — بخش ۲.۴. |
| ۴ | ✅ **تازه:** آیا پیامد نقض شرط جغرافیایی باید از همان مسیر liveness موجود (دموت بعد از `inactivityThreshold`، فعلاً ~۱ ساعت) بگذرد، یا نیاز به یک مسیر پیامدی جدا و فوری‌تر (مثلاً تابع `reportGeoViolation()` در `ValidatorsRegistry.sol`) دارد؟ بخش ۶.۵ برای جزئیات. |
| ۵ | ✅ **تازه:** آیا در کنار این چک فنی (که با VPN/پروکسی قابل‌دورزدن است)، یک تعهدنامه‌ی خارج از زنجیره هم از ولیدیتورها گرفته شود؟ |
