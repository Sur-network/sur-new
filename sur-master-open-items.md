# فهرست جامع کارهای باقی‌مانده — پروژه‌ی سور

> این سند همه‌ی موارد باز پروژه را یک‌جا جمع می‌کند. هر ردیف به سند مبدأ ارجاع می‌دهد.

---

## ۱. پارامترهای امنیتی فنی — تصمیم مستقیم شما لازم است

| مورد | سند مبدأ |
|---|---|
| درصد دقیق `slashBps` (جریمه‌ی غیرفعالی ولیدیتور) | `sur-tokenomics.md`, `sur-organizational-structure.md` |
| مقدار دقیق دوره‌ی cooldown خروج ولیدیتور | `sur-tokenomics.md` |
| آستانه‌ی دقیق ورود و درصد کارمزد عضویت (`membershipFeeBps`) — ✅ **به‌روزشده (این ردیف خودش چند دور از قلم افتاده بود):** مقادیر نهایی از کد فعلی (۵۰۰,۰۰۰ سورن پایه، ۴٪ کارمزد) — نه ۲ میلیونی که قبلاً اینجا نوشته شده بود؛ آن عدد به تصمیم اقتصادی بعدی (کاهش پایه برای کم‌کردن مانع ورود) تعلق داشت و اینجا هرگز به‌روز نشده بود | `sur-tokenomics.md` بخش ۶ |
| آیا نصاب‌های داخلی هیأت‌مدیره‌ی ولیدیتورها (فعلاً اکثریت ساده برای همه‌چیز) باید برای اختیارات حساس‌تر بالاتر باشد؟ | `sur-organizational-structure.md` |

## ۲. سرویس‌دهنده‌های خارجی — هنوز انتخاب نشده‌اند

| مورد | سند مبدأ |
|---|---|
| سرویس‌دهنده‌ی eKYC (RFP آماده است، منتظر پاسخ/انتخاب) | `sur-ekyc-rfp.md`, `sur-identity-registry-spec.md` |
| سرویس‌دهنده‌ی پیامک/تلگرام برای OTP | `sur-verifier-service-spec.md` |
| درگاه پرداخت برای `SurenSale` (زرین‌پال یا جایگزین) | `sur-suren-sale-spec.md` |
| شرکت/تیم حسابرسی امنیتی قراردادها | `sur-security-audit-plan.md` |

## ۳. محتوای سپیدنامه — وضعیت به‌روزشده

| بخش | وضعیت |
|---|---|
| فصل ۱ (چکیده) | ✅ **تکمیل شد** — بازنویسی شد به سه پاراگراف جذاب‌تر برای مخاطب عمومی، شامل اشاره‌ی صادقانه به شکست نسخه‌ی اول |
| فصل ۳ (پیشینه‌ی پروژه) | ✅ **تکمیل شد** — تاریخچه‌ی شبکه‌ی اول، دلیل توقف، درس‌های گرفته‌شده، با تحقیق واقعی از سپیدنامه‌ی قدیمی v0.5.0 |
| فصل ۱۰ (نقشه‌ی راه) | ✅ **تکمیل شد** — ۴ فاز مشخص؛ 🔶 فقط تاریخ‌های دقیق عمداً خالی مانده‌اند (باید خودتان تکمیل کنید) |
| فصل ۱۱ (تیم و مشاوران) | ✅ **تکمیل شد** — اسامی کامل هیأت‌مدیره (۷ نفر)، سایر اعضای هیأت مؤسس (۸ نفر)، و تیم فنی (۷ نفر) اضافه شد؛ 🔶 فقط بیوگرافی تفصیلی هرکدام برای نسخه‌ی نهایی عمومی باقی مانده |
| فصل ۱۲ (جنبه‌های حقوقی) | ✅ **بخش بزرگی تکمیل شد** — «حدود مسئولیت‌ها» نوشته شد، نوع دقیق ثبت حقوقی (مؤسسه‌ی غیرتجاری + شناسه‌ی ملی/شماره‌ی ثبت) هم تأیید شد؛ ✅ **تصحیح مهم:** «غیرتجاری» ≠ «غیرانتفاعی» — چنین مؤسسه‌ای می‌تواند سود داشته باشد، فقط نه از راه «تجارت»؛ 🔶 سؤال باقیمانده‌ی دقیق‌تر: کدام مکانیزم‌های درآمدی/تقسیم‌سودِ مشخص «تجارت» محسوب می‌شوند — نیازمند بررسی مورد‌به‌مورد وکیل مستقل، نه یک بررسی کلی تناقض |
| فصل ۱۳ (منابع و راه ارتباطی) | 🔶 کاملاً خالی |
| کاربردهای سورن فراتر از فی/وثیقه (DeFi، پرداخت) | 🔶 هنوز مشخص نشده |

## ۴. تصمیمات حقوقی/اساسنامه‌ای بنیاد — ✅ همه‌ی موارد زیر حل شدند

| مورد | تصمیم | جزئیات |
|---|---|---|
| آیا کمیته‌ی حقوقی/تطبیق قانونی جدا لازم است؟ | ✅ بله — و یک کمیته‌ی مالی/حسابرسی جدا هم لازم است | `sur-organizational-structure.md` |
| آیا بنیاد کارمند رسمی حقوق‌بگیر دارد یا کاملاً داوطلبانه است؟ | ✅ بله، کارمند رسمی حقوق‌بگیر دارد | `sur-organizational-structure.md` |
| آیا مجمع ولیدیتورها نیاز به پوشش حقوقی رسمی دارد؟ | ✅ خیر | `sur-organizational-structure.md` |
| آیا ولیدیتور باید شخص حقوقی باشد یا حقیقی هم مجاز است؟ | ✅ هردو مجاز است | `sur-organizational-structure.md` |
| صلاحیت قضایی و زبان قرارداد Validator Agreement | ✅ فعلاً بررسی/تعیین صلاحیت قضایی لازم نیست — ⚠️ ریسک آگاهانه‌پذیرفته‌شده، مخاطراتش مستند شده | `sur-organizational-structure.md`, `sur-whitepaper.md` (فصل ۹) |
| مکانیزم قیمت‌گذاری سهم‌الشرکه‌ی عضو جدید بنیاد | ✅ هر بار موکول به تصمیم اعضای بنیاد | `sur-organizational-structure.md` |
| آیا سقف مدت عضویت/مالکیت بنیاد وجود دارد؟ | ✅ خیر | `sur-organizational-structure.md` |
| نوع دقیق ثبت حقوقی بنیاد | ✅ مؤسسه‌ی غیرتجاری، شناسه‌ی ملی ۱۴۰۱۰۵۷۴۴۴۵، شماره‌ی ثبت ۵۲۷۲۵ | `sur-organizational-structure.md`, `sur-whitepaper.md` (فصل ۱۲) |

🔶 **یک تنش حقوقی مجزا هنوز باز مانده** (جزو موارد بالا نبود): آیا ماده‌ی ۳۱ اساسنامه (تقسیم سود بین شرکا) با ماهیت غیرتجاری/غیرانتفاعی بنیاد (ماده ۲) در تضاد حقوقی است؟ نیاز به بررسی وکیل مستقل — `sur-organizational-structure.md`.

## ۵. مزیت‌های رقابتی و ایده‌های کسب‌وکاری — ساختار تازه (به‌روزرسانی مهم)

⚠️ این بخش کاملاً بازسازی شده. حالا **دو سند مجزا** با هدف متفاوت وجود دارد:

- **`sur-competitive-advantages.md`** — فقط چیزهایی که *خودِ پلتفرم سور* را متمایز می‌کنند (نه ایده‌ی کسب‌وکاری قابل‌ساخت). ۱۲ مورد موجود + ۷ مورد بالقوه.
- **`sur-dapp-business-ideas.md`** — ۱۰۰ ایده‌ی کسب‌وکاری/دپ که *می‌توان* روی سور ساخت (نامش عمداً از «مزیت رقابتی» به این تغییر کرد، چون این دو مفهوم فرق دارند).

اولویت‌های عملی از این دو سند:

| ایده | وضعیت |
|---|---|
| رجیستری هویت مستقل (`IdentityRegistry`) | ✅ پیاده‌سازی شده و کاملاً از `ValidatorsRegistry` مستقل است |
| تراکنش‌های محرمانه (Zether) | 🔴 پیشنهاد و اسکلت فنی آماده (بخش ۶ همین سند را ببین) — نیازمند فورک پیاده‌سازی آدیت‌شده |
| قالب «DAO آماده» برای سازمان‌های دیگر | ✅ آماده‌ترین ایده — کد `FoundationDAO`/`ValidatorsBoard` از قبل نوشته شده |
| سرویس ثبت‌زمان/نوتاریزه‌کردن اسناد | ✅ می‌تواند مستقل شروع شود |
| سرویس نام‌گذاری (شبیه ENS) | ✅ کد متن‌باز آماده برای فورک |
| داوری/حل اختلاف عمومی | 🔶 نیازمند طراحی از پایه |
| Oracle بومی قیمت | 🔶 نیازمند طراحی سرویس؛ 🔶 پیشنهاد جدید: فورک نرم‌افزار نود متن‌باز Chainlink به‌جای طراحی از صفر |
| لایه‌ی اعتبار/رپوتیشن | 🔶 ایده‌ی اولیه؛ 🔶 پیشنهاد جدید: مدل attestation (الگوی Sismo) |
| ابزار/مستندات توسعه‌ی فارسی | 🔶 تصمیم عملیاتی بنیاد |
| ۹۱ ایده‌ی دیگر (فاکتورینگ، کیف‌پول ERC-4337، DEX، وام‌دهی، و غیره) | فهرست کامل با جزئیات در `sur-dapp-business-ideas.md` |

## ۶. تراکنش‌های محرمانه (Zether) — کار تازه‌ی این نشست

| مورد | وضعیت |
|---|---|
| پیشنهاد فنی و معماری | ✅ کامل — `sur-zether-confidential-transfers-proposal.md` |
| قرارداد اسکلت (`SurZether.sol`) | ✅ نوشته و کامپایل‌شده (بدون `viaIR`)، هر دو زبان | 
| راهنمای کاربرد | ✅ کامل — `sur-zether-usage-guide.md` |
| 🔴 انتخاب و فورک واقعی پیاده‌سازی آدیت‌شده‌ی `IZetherVerifier` | باز — `ConsenSys/anonymous-zether` پیشنهاد شده، ولی **هنوز فورک/آدیت نشده** |
| 🔴 بررسی سازگاری منحنی/precompile با پیاده‌سازی انتخابی | باز |
| 🔴 بازبینی امنیتی مستقل، قبل از هر دیپلوی واقعی | باز — الزامی |
| 🔴 توجه: ممکن است دیپلوی `ZetherVerifier` از سقف گس یک بلاک تجاوز کند (تجربه‌ی مستند دیگران) — نیازمند تست زودهنگام روی سور | باز |

## ۷. بازی‌های بلاک‌چینی — کار تازه‌ی این نشست (اختیاری، اولویت پایین)

✅ سند کامل — `sur-blockchain-games-proposal.md` — ۳۶ بازی/چارچوب EVM واقعاً موجود و متن‌باز، هرکدام با لینک گیت‌هاب. تصمیم اجرا (کدام بازی، چه زمانی) هنوز گرفته نشده — این یک ایده‌ی اکوسیستمی است، نه پیش‌نیاز راه‌اندازی شبکه.

## ۸. تصمیمات محصول/UX

| مورد | سند مبدأ |
|---|---|
| اپ Verifier: وب‌اپ ساده یا اپ موبایل بومی؟ | ✅ حل شد — وب‌اپ واکنش‌گرا، `sur-validator-app-claudecode-brief.md` |
| اپ احراز هویت عمومی: وب‌اپ یا بومی؟ | ✅ حل شد — `sur-identity-app-claudecode-brief.md` |
| 🔶 منوی تازه‌ی «مزیت‌ها و خدمات» در سایت — کدام از ۱۰۰ ایده در فاز اول سایت نمایش داده شود؟ | `sur-website-sitemap-proposal.md` |

## ۹. بودجه و مالی

| مورد | سند مبدأ |
|---|---|
| تصمیم بنیاد درباره‌ی زمان/روش دقیق درخواست بودجه‌ی تکمیلی | `sur-tokenomics.md`, `sur-marketing-roadmap.md` |
| اسامی دقیق افراد چرخه‌ی on-call/پیجینگ (Verifier + RewardRouter) | `sur-verifier-service-spec.md` |
| بودجه‌ی حسابرسی امنیتی از کدام منبع تأمین شود | `sur-security-audit-plan.md` |
| آیا برنامه‌ی جایزه‌ی باگ اجرا می‌شود؟ | `sur-security-audit-plan.md` |

## ۱۰. جزئیات فنی باقی‌مانده

| مورد | سند مبدأ |
|---|---|
| نصاب دوسوم برای چرخش `paymentOracle` در `SurenSale` اگر مبالغ بزرگ‌تر شود | `sur-suren-sale-spec.md` |
| عدد دقیق مدت نگه‌داری داده‌ی خام KYC | `sur-identity-registry-spec.md` |
| بازبینی دستی برای موارد پرریسک بازیابی هویت | `sur-identity-registry-spec.md` |
| اجرای واقعی ابزار ساخت genesis.json (مشخصات فنی آماده؛ فقط پیاده‌سازی باقی مانده) — ⚠️ **یادآوری صادقانه:** خودِ ابزار هنوز نوشته/اجرا نشده، و نتیجه‌ی تست `--verify` روی یک Besu واقعی هنوز وجود ندارد؛ «سند کامل شد» به‌معنی «genesis آماده و آزموده‌شده» نیست | `sur-genesis-builder-tool-spec.md` |
| ✅ هر ۸ پارامتر امنیتی چرخه‌ی عمر ولیدیتور نهایی شد: `maxEntriesPerWindow=1`, `entryWindowSeconds=۲۴ساعت`, `probationPeriod=۱هفته`, `requiredLivenessRatioBps=۹۵٪` (✅ **بازطراحی‌شده از یه شمارش خام قدیمی — به نسبت موفقیت، چون شمارش خام از گزارش منفی کاملاً بی‌تأثیر بود؛ جزئیات کامل در `sur-tokenomics.md` بخش ۶**), `inactivityThreshold=۱ساعت`, `recoveryPeriod=۴۸ساعت`, `slashBps=۱٪`, `exitCooldown=۱هفته` | `ValidatorsRegistry.sol` |
| 🔶 مبلغ کل دقیق و فهرست آدرس‌به‌آدرس ردیف «جبران ولیدیتورهای شبکه‌ی قبلی» | `sur-tokenomics.md` |
| به‌روزرسانی جدول پیش‌بینی عرضه‌ی کل بعد از مشخص‌شدن مبلغ ردیف «جبران ولیدیتور» | `sur-tokenomics.md` |

---

## ۱۱. بازبینی اقتصادی/تضاد منافع — کار تازه‌ی این نشست (همه حل شدند)

طی یه بحث عمیق درباره‌ی ریسک‌های مالی/انگیزشی توکنومیکس، ۶ تصمیم گرفته شد:

| مورد | تصمیم نهایی | جزئیات |
|---|---|---|
| بودجه‌ی جاری بنیاد | ✅ ۱۵٪ از **کل** بلاک‌ریوارد (نه از سهم ۵۰٪ خزانه — مستقل و بدون‌ارتباط با هر تغییر آینده در نسبت خزانه/ولیدیتور)، خودکار و غیرقابل‌لغو (۳,۱۵۳,۶۰۰ سورن/سال)، فقط برای حقوق/هزینه‌ی جاری | `sur-tokenomics.md` بخش ۶.۵ |
| مقصد کارمزد عضویت ولیدیتور | ✅ ۱۰۰٪ مستقیم بین ولیدیتورهای فعال (نه خزانه) — برای انگیزه‌ی تشویق عضویت تازه | `sur-tokenomics.md` بخش ۶ |
| مکانیزم قیمت دوره‌ی گذار پس از پایان قیمت ثابت | ✅ پنجره‌ی ۴۵روزه، قیمت‌گذاری پویا و متقارن (±۰.۵٪ روزانه بر اساس حجم فروش نسبت به میانگین ۷روزه)، کف ۹۰ت/سقف ۱۵۰ت | `sur-tokenomics.md` بخش ۳ |
| استیک برای دسترسی به سرویس‌ها (Service Staking) | ✅ مفهوم و جدول کامل ۶ سرویس (نام‌گذاری، ثبت‌زمان، صدور مدرک، رجیستری کسب‌وکار، Zether، اعتبار/رپوتیشن) | `sur-tokenomics.md` بخش ۱۰، `sur-dapp-business-ideas.md` |
| نسبت سهم مستقیم ولیدیتور در برابر خزانه | ✅ دیگر عدد ثابت نیست — رأی‌گیری دومجلسی (اکثریت هیأت‌مدیره **و** دوسوم مجمع کامل، هردو لازم)، بازه‌ی مجاز ۴۰٪-۶۵٪ (شروع ۵۰٪)، فاصله‌ی حداقلی ۶ ماه بین تغییرات | `sur-tokenomics.md` بخش ۶.۶ |
| پارامترهای منحنی وثیقه‌ی ورود | ✅ پایه از ۲ میلیون به **۵۰۰,۰۰۰ سورن**؛ دوره‌ی دوبرابرشدن از ۱۶ به **۴۰ ولیدیتور پرداخت‌کننده** — برای کاهش مانع سرمایه‌ای ورود و ریسک تمرکز (بدون تغییر نقطه‌ی سربه‌سر سودآوری خالص، که مستقل از این دو پارامتره) | `sur-tokenomics.md` بخش ۶ |
| سوزاندن فی | ✅ ۳۰٪ ثابت از **فی معمولی تراکنش** (نه کارمزد عضویت — که کاملاً معاف است، بخش ۱۳ پایین) هر epoch سوزانده می‌شود؛ ۷۰٪ باقی‌مانده مثل قبل بین ولیدیتورها تقسیم می‌شود. نرخ فعلاً **ثابت و غیرقابل‌تغییر** (تصمیم آگاهانه — هیچ نهاد بی‌طرفی برای نظارت بر تغییرش شناسایی نشد) | `sur-tokenomics.md` بخش ۷ |
| الزام حضور فیزیکی نود در ایران | ✅ شرط تازه‌ی عضویت — نود باید همیشه داخل ایران باشد؛ احراز مداوم توسط `verifier` (IP-geolocation روی همون داده‌ی liveness). 🔶 **باز:** آیا پیامد فوری لازمه یا مسیر liveness موجود (تأخیر ~۱ ساعت) کافیه؟ | `sur-tokenomics.md` بخش ۶.۷، `sur-blockchain-design-doc.md` بخش ۶، `sur-verifier-service-spec.md` بخش ۶.۵ |

🔴 **پیامد فنی مهم — نیازمند تغییر قرارداد:**
- `BlockRewardDistributor.sol` — افزودن مقصد سوم (بنیاد، ۱۵٪ از کل)؛ تبدیل `TREASURY_SHARE_BPS` ثابت به `validatorDirectShareBps` قابل‌حکمرانی؛ افزودن سازوکار کامل رأی‌گیری دومجلسی (`proposeShareChange`/`boardVoteShareChange`/`validatorVoteShareChange`)؛ ✅ **تازه‌تر:** افزودن سوزاندن ۳۰٪ ثابت فی (`FEE_BURN_BPS`, `BURN_ADDRESS`, رویداد `FeesBurned`). **هر دو زبان کامل و کامپایل‌شده.**
- `ValidatorsRegistry.sol` — تغییر مقصد کارمزد عضویت از خزانه به تقسیم مستقیم بین ولیدیتورهای فعال؛ افزودن `isPaidEntrant`/`paidValidatorCount` (منحنی وثیقه دیگر ۷ ولیدیتور مؤسس رایگان را نمی‌شمرد)؛ تغییر `entryThresholdBase` به ۵۰۰,۰۰۰ و `growthFactorPerValidator` برای دوره‌ی دوبرابرشدن ۴۰ (به‌جای ۱۶). **هر دو زبان کامل.**
- `SurenSale.sol` — افزودن منطق ردیابی حجم فروش روزانه + میانگین متحرک برای دوره‌ی گذار (نه فقط فرمول زمانی محض فعلی). **هر دو زبان کامل.**
- قرارداد تازه‌ی `ServiceStaking.sol` — نوشته و کامپایل شد (اسکلت پیش‌نویس اول، مشابه SurZether.sol؛ الگوی قفل/آزادسازی `ValidatorsRegistry` را بازاستفاده می‌کند). **هر دو زبان کامل.**
- 🔶 **باز:** الزام حضور فیزیکی نود در ایران — فعلاً فقط مستندسازی شده (بخش‌های بالا)؛ هیچ تغییر قراردادی برایش هنوز اعمال نشده (پیامدش از مسیر `reportLiveness` موجود عبور می‌کند، بدون نیاز به تابع تازه — مگر تصمیم به مسیر پیامدی فوری‌تر گرفته شود).

## ۱۲. ریسک‌های عمومی صنعت — از بررسی نظرات Gemini، تأییدشده به‌عنوان خلأهای واقعی

طی بررسی یه نقد بیرونی (Gemini) روی کل پروژه، اکثر ادعاهاش (تناقض بین اسناد) کاملاً ساختگی از آب دراومدن — جزئیات کامل در `critiques/gemini-review-assessment.md`. ولی ۵ مورد از نگرانی‌هاش، با این‌که مخصوص این پروژه نبودن (نگرانی‌های عمومی و شناخته‌شده‌ی صنعت بلاک‌چین)، **جایی توی اسناد پروژه به‌صراحت پوشش داده نشده بودن** — پس به‌عنوان آیتم باز ثبت می‌شن:

| # | ریسک | چرا وارده | وضعیت |
|---|---|---|---|
| ۱ | ریسک تمرکز شبکه از اقتصاد ولیدیتور | نتیجه‌گیری Gemini درست بود (دلیلش غلط بود — گفت «هزینه‌ی سخت‌افزار»، ولی سور QBFT است نه PoW؛ مانع واقعی «هزینه‌ی سرمایه‌ی وثیقه»ست). خودِ این تنش از قبل در `sur-tokenomics.md` («تنش اقتصادی شناخته‌شده») مستند بود، ولی هیچ راه‌حل مشخصی براش نوشته نشده | 🔶 باز |
| ۲ | ریسک نقدینگی راه‌اندازی DEX (Liquidity Bootstrapping / Slippage) | `sur-dex-launch-plan.md` این ریسک خاص (خروج نقدینگی‌دهندگان اولیه، Slippage بالا در نبود نقدینگی کافی) رو صریح مطرح نکرده | 🔶 باز |
| ۳ | تعارض Zether با الزامات AML/KYC | یه تنش شناخته‌شده در کل صنعت بلاک‌چین (نه مخصوص سور)؛ چون Zether صراحتاً «هنوز آماده‌ی دیپلوی واقعی نیست»، این نگرانی زودهنگامه ولی باید قبل از هر دیپلوی واقعی به یه سند حقوقی/تطبیقی مشخص تبدیل بشه | 🔶 باز — وابسته به تصمیم اجرای Zether (بخش ۶) |
| ۴ | ریسک تبانی ۱/۳ در multisig پل ارتباطی (Bridge) | `sur-bridge-risk-analysis.md` آستانه‌ی دقیق تبانی/لورفتن کلید نگهبانان رو صریح مطرح نکرده — یه ریسک عمومی هر سیستم multisig/BFT | 🔶 باز |
| ۵ | گس بالای تراکنش‌های Zether و افت TPS | درست و شناخته‌شده در سطح فناوری ZK به‌طور کلی؛ سند مصرف Zether این اثر رو با عدد مشخص بررسی نکرده | 🔶 باز — وابسته به تصمیم اجرای Zether (بخش ۶) |

## ۱۳. اصلاحات امنیتی بحرانی قرارداد — از یک بازبینی مستقل (Final Consistency Audit)

یک بازبینی مستقل و عمیق (نه فقط جست‌وجوی عبارت‌های قدیمی، بلکه بررسی *تعامل* منطق قراردادها) چهار مشکل بحرانی واقعی پیدا کرد که همه اصلاح، کامپایل، و در هر دو زبان هم‌ترازسازی شدند:

| # | مشکل | فایل | اصلاح |
|---|---|---|---|
| ۱ | باگ integer division: تعدیل روزانه‌ی ±۰.۵٪ قیمت دوره‌ی گذار برای هر قیمتی بین ۹۰-۱۵۰ تومان همیشه صفر می‌شد (`price×50/10000` رند به صفر) — یعنی **کل مکانیزم تعدیل هرگز کار نمی‌کرد** | `SurenSale.sol` | قیمت داخلی حالا با دقت ۱۰۰۰برابر (میلی‌تومان) نگه‌داری می‌شود؛ `currentPriceToman()` بدون تغییر در رابط بیرونی |
| ۲ | اکسپلویت استیک Reputation: `stakedAt` فقط در اولین استیک تنظیم می‌شد، نه در top-up — یعنی می‌شد مبلغ ناچیز استیک کرد، ۹۰ روز صبر کرد، بعد مبلغ عظیم top-up کرد و بلافاصله برداشت | `ServiceStaking.sol` | هر `stake()` برای Reputation، ساعت ۹۰روزه را ریست می‌کند |
| ۳ | ناهماهنگی genesis/storage: ترتیب `paidValidatorCount`/`validators` بین دو زبان جابه‌جا بود؛ فایل کمکی `ValidatorsRegistry_GenesisSeed.sol` هم فیلد `isPaidEntrant` را نداشت هم دو متغیر اسکالر جدید (`paidValidatorCount`, `verifier`) را کلاً جا انداخته بود — می‌توانست باعث نوشتن داده‌ی آرایه‌ی ولیدیتورهای مؤسس در اسلات اشتباه شود | `ValidatorsRegistry.sol` + `genesis-seed-helpers/ValidatorsRegistry_GenesisSeed.sol` | ترتیب هم‌ترازسازی شد؛ فایل کمکی کاملاً بازنویسی و مستند شد |
| ۴ | رأی حکمرانی مانده‌شده (stale vote): در ۴ مکانیزم رأی‌گیری مجزا، شمارنده‌ی رأی فقط زیاد می‌شد ولی آستانه‌ی لازم هر بار زنده از تعداد فعال *فعلی* حساب می‌شد — یعنی یک پیشنهاد ردشده می‌توانست بعداً، بدون هیچ رأی تازه‌ای، فقط به‌خاطر کوچیک‌شدن شبکه، خودبه‌خود قابل‌اجرا شود | `BlockRewardDistributor.sol` (رأی‌گیری دومجلسی)، `ValidatorsRegistry.sol` (پارامترهای امنیتی)، `ValidatorsTreasury.sol` (۲ مکانیزم: خرج خزانه + سقف بودجه‌ی هیأت‌مدیره)، `ValidatorsBoard.sol` (اقدامات هیأت) | آستانه‌ی لازم در لحظه‌ی ثبت پیشنهاد snapshot/ثابت می‌شود؛ + مهلت انقضا (۱۴ تا ۳۰ روز) اضافه شد. ⚠️ **تصحیح‌شده:** برای `ValidatorsBoard.sol` این‌جا اشتباهاً نوشته شده بود «فقط انقضا کافیه چون مخرجش ثابته» — این استدلال غلط بود (به ردیف بعدی مراجعه کن که چرا)؛ اونجا هم snapshot واقعی لازم بود و اضافه شد |

🔴 **هر ۴ مورد کاملاً اصلاح، کامپایل‌شده (تکی و در کنار کل ۹ قرارداد)، و در هر دو زبان هم‌ترازسازی شده‌اند.**

✅ **موارد ۱-۶ این بازبینی انجام شد:** کارمزد عضویت داخل استخر burn در `sur-tokenomics.md` (بررسی شد، از قبل تمیز بود)؛ عدد TPS از ۰.۱۶ به ۰.۲۲۷ اصلاح شد؛ مقصد کارمزد عضویت در `sur-contracts-oracles-accounts-report.md`/`sur-contracts-deploy-notes.md` اصلاح شد؛ ادعای مطلق «بنیاد هیچ oracleای کنترل نمی‌کند» در `sur-blockchain-design-doc.md` (۲ مورد جدید) اصلاح شد؛ بخش «کشف قیمت» یک فاز کامل (گذار ۴۵روزه) را جا انداخته بود، اضافه شد؛ دیاگرام SVG منحنی وثیقه با پارامترهای تازه کاملاً بازسازی شد.

✅ **موارد ۷-۹ این بازبینی هم انجام شد:**
- **گرد شدن به Treasury:** به‌جای دست‌زدن به ۱۰ فایل (که در سطح خلاصه «۱۰۰٪» کاملاً درستن)، توضیح دقیق رونداری فقط به سند فنی مرجع (`sur-smart-contracts-reference.md`) اضافه شد — مبلغ به‌قدری ناچیزه (کمتر از یک wei به‌ازای هر ولیدیتور) که تصمیم گرفته شد کد پیچیده نشود، فقط دقیقاً مستند شود.
- **دقت زمانی «فوری»:** ۴ نمونه (۳ در `sur-tokenomics.md`، ۱ در `sur-organizational-structure.md`) که کارمزد عضویت را «بلافاصله» توصیف می‌کردند، اصلاح شدند — واقعیت این است که پرداخت در epoch توزیع بعدی (~۲۳ ساعت بعد) به ولیدیتورهای فعالِ *آن‌موقع* می‌رسد، نه لزوماً دقیقاً همان مجموعه‌ای که لحظه‌ی ورود فعال بودند.
- **تعریف `paidValidatorCount`:** کامنت قرارداد (هر دو زبان) اصلاح شد — این متغیر واقعاً «ولیدیتور پرداخت‌کننده‌ی *فعلاً فعال*» نیست، بلکه «پرداخت‌کننده‌ای که هنوز خروج نکرده» است (Probation/Active/Demoted را یکسان شامل می‌شود). این رفتار عمدی است، فقط توصیفش نادقیق بود.

✅ **موارد ۱۰-۱۱ هم نهایی شد:**
- **Guardrail هیأت‌مدیره:** بازه‌ی سخت برای هر سه پارامتر اقتصادی ورود (`entryThresholdBase`: ۱۰۰K-۲M، `membershipFeeBps`: ۱٪-۱۰٪، `growthFactorPerValidator`: دوره‌ی دوبرابرشدن هر ۲۰ تا ۸۰ نفر) + یک تایمر مشترک ۱۸۰روزه بین هر تغییر در *هرکدام* از این سه (نه سه تایمر جدا) اضافه شد — در `ValidatorsRegistry.sol`، هر دو زبان، کامپایل‌شده.
- **سقف تعداد ولیدیتور:** تأیید شد هدف رسمی همان ۵۰ تا حداکثر ۱۰۰ باقی می‌ماند؛ منحنی فعلی دقیقاً برای همین بازه بهینه است و آگاهانه فراتر از آن طراحی نشده — این صریحاً در `sur-tokenomics.md` بخش ۶ مستند شد تا به‌عنوان یک نقص فراموش‌شده تلقی نشود.

⚠️ **تصحیح مهم (بعد از یک دور بازبینی مستقل دیگر):** ادعای «کل ۱۱ مورد بسته شد» زودهنگام بود — یک بازبینی مستقل دیگر روی همین zip، **دو باگ بحرانی دیگر** پیدا کرد که کاملاً از قلم افتاده بودند:

| باگ | فایل | اصلاح |
|---|---|---|
| اصلاح `SurenSale` فقط نصفه بود: `reportPayment()` هنوز از قیمت *نمایشی* رند‌شده (نه دقت کامل داخلی) برای محاسبه‌ی *واقعی* تسویه استفاده می‌کرد — یعنی خریدار همچنان با پله‌های کامل تومان تسویه می‌شد، نه گام ۰.۵٪ مقصود. **باگ دومی هم پیدا شد:** `currentPriceMilliToman()` اصلاً `isSaleActive()` را چک نمی‌کرد | `SurenSale.sol` | `reportPayment()` مستقیماً از `currentPriceMilliToman()` استفاده می‌کند؛ تابع دوم هم برای دوره‌ی ثابت اصلاح شد |
| `FoundationDAO.sol` کاملاً از اصلاح رأی‌مانده‌شده جا مانده بود — همان الگوی زنده‌محاسبه‌شدنِ آستانه، بدون snapshot یا انقضا | `FoundationDAO.sol` | snapshot + انقضای ۳۰روزه اضافه شد |
| اصلاح `ValidatorsBoard` قبلی ناقص بود: استدلال «denominator ثابته» غلط بود — `refreshBoard()` می‌تواند کمتر از ۵ عضو انتخاب کند، پس `boardMembers.length` واقعاً متغیر است | `ValidatorsBoard.sol` | این‌بار snapshot واقعی (نه فقط انقضا) اضافه شد |

🔴 **هر ۳ مورد کاملاً اصلاح، کامپایل‌شده (هر دو زبان).**

✅ **همه‌ی ۵ مورد باقیمانده‌ی همین دور بازبینی هم انجام شد:**
- `sur-genesis-builder-tool-spec.md` — بخش ۴.۲.۱ تازه اضافه شد: الگوریتم سه‌مرحله‌ای (بایت‌کد + storage پیچیده + overlay اسکالرها) برای `ValidatorsRegistry`، به‌همراه یک فهرست کامل post-build assertions (که ابزار باید خودکار اجرا و در صورت شکست، ساخت genesis را متوقف کند)
- `sur-validator-yield-analysis.md` — کل جدول با یک تفسیر یکپارچه («ولیدیتور تازه‌ای که وقتی N نفر موجودند می‌پیوندد، درآمدش با N+1 محاسبه می‌شود») بازنویسی شد؛ سربه‌سر عملیاتی به N≈۲۸-۲۹ به‌روز شد
- `MAX_GROWTH_VALIDATORS=1280` — کامنت (هر دو زبان) اصلاح شد تا صادقانه بگوید حداکثر ضریب واقعی با `growthFactor` فعلی فرق می‌کند (۲^۱۶ تا ۲^۶۴)، نه همیشه ۲^۳۲؛ و توضیح دهد چرا این هیچ ریسک سرریز واقعی ایجاد نمی‌کند
- سپیدنامه — «بدون مکانیزم سوزاندن» و ادعای مطلق «هیچ اوراکلی» اصلاح شد
- `sur-blockchain-design-doc.md` — ادعای «مدل نهایی ۵۰٪/۵۰٪» اصلاح شد
- کامنت‌های قدیمی در `ValidatorsRegistry.sol` (۲ مورد) و `BlockRewardDistributor.sol` (۱ مورد) — هر دو زبان، اصلاح شد

🔴 همه‌ی این موارد کامپایل تأیید شده‌اند (تکی و در کنار کل ۹ قرارداد).

✅ **یک دور بازبینی مستقل دیگر، ۶ مورد باقیمانده‌ی جزئی‌تر رو هم پیدا کرد که همه اصلاح شد:**
1. ادعای «همیشه ۲^۳۲» در ۳ محل (`sur-tokenomics.md` ×۲، `sur-blockchain-design-doc.md` ×۱) — با توضیح صادقانه‌ی بازه‌ی واقعی (۲^۱۶ تا ۲^۶۴) جایگزین شد
2. ادعای مطلق «بنیاد هیچ اوراکلی کنترل نمی‌کند» در ۵ محل (`FoundationDAO.sol` هر دو زبان، `sur-smart-contracts-reference.md` ×۲، `sur-organizational-structure.md`) — استثنای `identityOracle`/`paymentOracle` اضافه شد
3. سند `sur-genesis-builder-tool-spec.md`: اندازه‌ی نادرست آرایه‌ی نمونه (۵→۷ اصلاح شد)؛ فهرست overlay اسکالر گسترش یافت تا همه‌ی پارامترهای امنیتی (`slashBps`, `exitCooldown`, و مشابه) + `windowStart` (از `network.genesisTimestamp`) را هم پوشش دهد؛ فهرست post-build assertions هم متناسب گسترش یافت
4. تناقض داخلی خودِ این فایل — ردیف قدیمی جدول بخش ۱۳ هنوز استدلال ردشده‌ی «مخرج ValidatorsBoard ثابته» را تکرار می‌کرد — اصلاح شد
5. چک‌لیست `sur-blockchain-design-doc.md` هنوز «مدل ۵۰٪/۵۰٪» قدیمی را داشت — به مدل سه‌مقصدی فعلی اصلاح شد
6. `sur-website-content.md` هنوز می‌گفت «بدون مکانیزم سوزاندن» — تفکیک شد (بلاک‌ریوارد سوزانده نمی‌شود، ۳۰٪ فی معمولی می‌شود)

🔴 همه‌ی موارد قراردادی این دور هم کامپایل تأیید شده‌اند.

🎉 ~~با این دو مورد، کل ۱۱ موردِ Final Consistency Audit بسته شد.~~ (تصحیح‌شده — بالا را ببینید)

## ۱۴. سه اصلاح امنیتی دیگر در `ValidatorsRegistry.sol` — از یک بازبینی بعدی

بعد از تثبیت مدل نسبتی ۹۵٪ برای liveness، یه بازبینی مستقل سه ایراد باقیمانده رو پیدا کرد که هرسه اصلاح شدند:

| # | مشکل | راه‌حل |
|---|---|---|
| ۱ | هیچ حداقل فاصله‌ی زمانی بین گزارش‌های شمرده‌شده نبود (یه Verifier خراب می‌تونست با گزارش سریع/تکراری نسبت رو مصنوعاً پر کنه)؛ + یه فیلد مشترک برای probation و recovery اجازه‌ی حداقل متفاوت نمی‌داد | `MIN_LIVENESS_CHECK_INTERVAL = ۵ دقیقه` اضافه شد؛ `requiredLivenessRatioBps` به دو پارامتر مستقل (probation/recovery) تقسیم شد |
| ۲ | `requestExit()` راه فراری از جریمه‌ی غیرفعالی بود — ولیدیتور می‌تونست دقیقاً قبل از دموت، بدون جریمه خارج بشه | موقع خروج، اگه از قبل واجد شرایط دموت بوده، همون جریمه اعمال می‌شه |
| ۳ | Verifier یه نقطه‌ی خطای مشترکه؛ خرابیش می‌تونست باعث جریمه‌ی دسته‌جمعی ولیدیتورهای سالم بشه | یه سوپاپ ایمنی اضافه شد — ولی **فقط جریمه** رو موقتاً متوقف می‌کنه وقتی الگوی مشکوک (>۲۰٪ دموت توی ۱ ساعت) تشخیص داده بشه؛ **حذف از لیست فعال همیشه و بدون قید انجام می‌شه** — چون این دقیقاً همون چیزیه که به QBFT اجازه می‌ده نصابش رو با استخر کوچیک‌شده هماهنگ کنه؛ متوقف‌کردنش ریسک ایستادن شبکه رو تشدید می‌کرد |

🔴 هر سه مورد در هر دو زبان پیاده‌سازی و کامپایل تأیید شده‌اند (تکی و در کنار کل ۹ قرارداد). جزئیات کامل استدلال هرکدوم در `sur-tokenomics.md` بخش ۶.

✅ **یه بازبینی بعدی، سه شکاف دیگه توی همین سه اصلاح پیدا کرد که همه رفع شدند:**
1. شرط ۹۵٪ حداقل نمونه نداشت — یه گزارش مثبت تنها می‌تونست نسبت ۱۰۰٪ بسازه. `MIN_CHECK_COVERAGE_BPS=50%` اضافه شد.
2. سند genesis-builder از قرارداد عقب مونده بود — `requiredRecoveryLivenessRatioBps` (پارامتر تازه‌ی جدا) در config/overlay/assertions نبود. اضافه شد.
3. سوپاپ جمعی به ترتیب تراکنش‌ها وابسته بود و جریمه‌های قبلی برنمی‌گشتن. **کاملاً بازطراحی شد:** جریمه دیگه فوری نیست — هر دموت توی یه «DemotionEpoch» ثابت ثبت می‌شه، و فقط بعد از بسته‌شدن کامل پنجره، یه تابع permissionless (`resolvePendingSlash`) تصمیم یکسان برای همه‌ی دموت‌شده‌های اون پنجره می‌گیره. `withdrawStake()` هم اصلاح شد تا نتونه جریمه‌ی معلق رو دور بزنه.

🔴 همه‌ی این سه اصلاح تکمیلی هم، هر دو زبان، کامپایل تأیید شده‌اند.

✅ **یه بازبینی دیگه، یه باگ فلج‌کننده + دو شکاف مرزی دیگه توی همین سه اصلاح پیدا کرد:**
1. **باگ عددی فلج‌کننده در حداقل نمونه:** محاسبه بر مبنای `MIN_LIVENESS_CHECK_INTERVAL` (۵ دقیقه، یه مرز throttle) بود، نه سرعت واقعی Verifier (۱۰-۱۵ دقیقه). نتیجه: یه ولیدیتور کاملاً سالم با سرعت ۱۵دقیقه‌ای مستندشده، هرگز نمی‌تونست به حداقل لازم برسه — یعنی **هیچ‌وقت نمی‌تونست ترفیع بگیره**. اصلاح شد: ثابت تازه `EXPECTED_VERIFIER_CADENCE_SECONDS=15دقیقه` (کندترین سرعت مستندشده) به‌عنوان مبنای محاسبه.
2. **ناسازگاری مخرج‌کسر بین `requestExit()` و `demoteForInactivity()`:** ترتیب فراخوانی `_recordDemotion()`/`_removeFromActive()` بین این دو مسیر برعکس بود، یعنی `referenceCount` یکی فرق می‌کرد — نزدیک مرز ۲۰٪ می‌تونست نتیجه رو عوض کنه. ترتیب هم‌ترازسازی شد.
3. **جریمه‌ی معلق گمشدنی:** `promoteAfterRecovery()` هیچ چکی روی `pendingSlashEpoch` نداشت — یه ولیدیتور می‌تونست با جریمه‌ی معلق حل‌نشده برگرده، دوباره دموت بشه، و ردیابی جریمه‌ی اول برای همیشه با overwrite شدن `pendingSlashEpoch` گم بشه. اضافه شد: `promoteAfterRecovery()` الان `pendingSlashEpoch == 0` رو شرط می‌کنه.

🔴 هر سه، هر دو زبان، کامپایل تأیید شده‌اند. جزئیات کامل در `sur-tokenomics.md` بخش ۶.

✅ **تصمیم قطعی شما درباره‌ی فاصله‌ی polling Verifier:** یه بازبینی دیگه نشون داد که رویکرد «حداقل نمونه» (که برای رفع باگ عددی به `EXPECTED_VERIFIER_CADENCE_SECONDS=۱۵دقیقه` تغییر کرده بود) خودش یه شکاف داره — چک‌ها می‌تونن قانوناً نزدیک انتهای دوره خوشه‌ای بشن. شما صریحاً تصمیم گرفتید: **هم شرط فاصله‌ی حداقلی هم شرط حداقل تعداد کل لازمه** (نه جایگزینی با یه مکانیزم «حداکثر فاصله»)، و فاصله‌ی واقعی Verifier رو **۱۰ دقیقه‌ی قطعی** (نه بازه‌ی ۱۰-۱۵) تعیین کردید. بر این اساس: `MIN_LIVENESS_CHECK_INTERVAL=۱۰دقیقه` شد (این دیگه *همون* فاصله‌ی واقعی Verifier است، نه یه throttle جدا)؛ `_minRequiredChecks()` مستقیماً از همین ثابت استفاده می‌کنه (نه یه ثابت جدا)؛ `sur-verifier-service-spec.md` هم برای فاصله‌ی قطعی ۱۰دقیقه‌ای به‌روز شد. محدودیت خوشه‌ای‌شدن آگاهانه به‌عنوان یه مصالحه‌ی طراحی پذیرفته شد، نه یه باگ که نیاز به اصلاح داشته باشه.

## ۱۵. مدل کسب‌وکار اکوسیستم — «چارچوب اعتبارسنجی» (Validation Engine) — کار تازه‌ی این نشست



از بررسی نظرات ChatGPT درباره‌ی «سور دقیقاً چه چیزی می‌فروشد؟»، یک تصمیم اجرایی مشخص گرفته شد:

✅ **تصمیم نهایی:** قبل از هرگونه سرمایه‌گذاری روی SDK/برنامه‌ی حمایتی/Accelerator برای اکوسیستم ۱۰۰ ایده (`sur-dapp-business-ideas.md`)، یک فاز اعتبارسنجی ۸-۱۲هفته‌ای با **یک نفر Owner از نیروی فعلی بنیاد** (نه تیم تازه، نه استخدام، نه مشاور بیرونی — که نیازمند بودجه‌ی تازه از خزانه‌ی ولیدیتورها بود) اجرا می‌شود: غربال ۱۰۰ ایده → ۱۰ نامزد → Customer Discovery + Builder Discovery موازی → حداقل ۱ فرصت با هم مشتری واقعی هم سازنده‌ی واقعی.

**خروجی دوگانه:** هم کشف کسب‌وکار (کدام فرصت‌ها تقاضای واقعی دارند)، هم کشف فنی (کدام زیرساخت‌ها — Identity، Naming، و مشابه — واقعاً توسط چند Builder مستقل درخواست می‌شن، نه فقط حدس تیم فنی).

**جزئیات کامل مکانیزم، جدول زمانی، دروازه‌های تصمیم (Gates)، و معیار موفقیت:** `sur-dapp-business-ideas.md` (بخش ابتدایی سند). یادداشت متقابل در `sur-product-development-plan.md` و یه KPI تازه در `sur-marketing-roadmap.md` هم اضافه شد.



تمام کارهای این نشست (اسناد Zether، ۱۰۰ ایده‌ی کسب‌وکاری، ۳۶ بازی، بازسازی مزیت‌های رقابتی، به‌روزرسانی سپیدنامه/سایت‌مپ، و همین سند) روی هارد لوکال (`D:\Amir\Business\SUR\NewSur\Plan`) هم نوشته شد. فایل قدیمی `sur-competitive-advantages-additional-proposals.md` به `_archive/` منتقل شد (نه حذف — طبق محدودیت کانکتور).

## خلاصه‌ی اولویت‌بندی پیشنهادی

🔴 **قبل از هرچیز:** پارامترهای امنیتی (بخش ۱) — چون حسابرسی امنیتی باید روی کد نهایی انجام شود.
🟡 **موازی با حسابرسی:** انتخاب سرویس‌دهنده‌های خارجی (بخش ۲).
🟢 **می‌تواند بعداً:** محتوای باقی‌مانده‌ی سپیدنامه (بخش ۳)، تصمیم اجرای Zether/بازی‌ها (بخش‌های ۶ و ۷) — این‌ها برای انتشار عمومی/رشد اکوسیستم لازم‌اند، نه برای راه‌اندازی فنی شبکه.
