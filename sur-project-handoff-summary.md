# سند تحویل وضعیت پروژه — بلاک‌چین سور (سورن)

> این سند برای این ساخته شده که در یک چت تازه با Claude، همین ابتدا کپی/پیست یا آپلود شود تا نیازی به خواندن مکالمه‌ی قبلی (که خیلی طولانی شده) نباشد. تاریخ نگارش: طبق آخرین وضعیت این گفتگو.

## ۱. پروژه چیست

بلاک‌چین خصوصی/کنسرسیومی **سور** (توکن بومی: **سورن**)، روی **Hyperledger Besu** با اجماع **QBFT**. بنیاد سور مسئول توسعه/اکوسیستم است؛ شبکه توسط یک مجمع ولیدیتور اداره می‌شود. یک نسخه‌ی قبلی این شبکه شکست خورده (متوقف شده)، و این پروژه بازطراحی کامل از صفر است.

## ۲. دسترسی فایل‌سیستم — منبع صحت کجاست

✅ **از این پس (طبق درخواست صریح کاربر)، تمام آرتیفکت‌های پروژه باید در مسیر زیر روی کامپیوتر کاربر ذخیره شوند، نه فقط در خروجی sandbox:**

```
D:\Amir\Business\SUR\NewSur\Plan
```

این مسیر یک **کانکتور فایل‌سیستم MCP** (ابزارهای `Filesystem:*`) در دسترس است — می‌توان مستقیم `read_text_file`, `write_file`, `list_directory`, `move_file`, `create_directory`, `search_files`, `edit_file` را روی آن صدا زد. این پوشه یک ریپوی Git هم هست (`.git` داخلش هست) — Claude به ابزار git دسترسی ندارد، فقط فایل‌سیستم خام.

⚠️ **محدودیت‌های شناخته‌شده‌ی این کانکتور:**
- هیچ ابزار «حذف واقعی» فایل وجود ندارد — فقط `move_file`. فایل‌های منسوخ به `D:\Amir\Business\SUR\NewSur\Plan\_archive\` منتقل می‌شوند، نه حذف.
- فقط فایل‌های **متنی** (`.md`, `.sol`, `.svg`) قابل‌نوشتن‌اند. فایل‌های باینری (`.png`, `.jpg`, `.pptx`, `.zip`) از این طریق قابل‌انتقال نیستند — باید از طریق لینک دانلود چت دستی ذخیره شوند.
- گاهی این کانکتور timeout می‌دهد یا مسیرهای مجازش عوض می‌شود (هر دو قبلاً در این پروژه اتفاق افتاده) — همیشه اول `Filesystem:list_allowed_directories` را چک کن تا مطمئن شوی مسیر درست در دسترس است، و بعد از هر `write_file` مهم، با یک `read_text_file` نتیجه را تأیید کن.
- `edit_file` گاهی به‌خاطر تفاوت‌های نامرئی encoding در متن فارسی/RTL با «no exact match» شکست می‌خورد — در آن صورت، کل فایل را با `write_file` بازنویسی کن، نه تلاش مکرر برای edit جزئی.

## ۳. ⚠️ نکته‌ی حیاتی: یک چت **دیگر** هم روی همین پروژه کار می‌کند

یک چت جدا به اسم **«راهنمای اینترفیس کاربری قراردادهای هوشمند»** (در claude.ai، لینک: `https://claude.ai/chat/cfb106a6-1123-4a4e-96d3-b774d9e98369`) به همین پوشه‌ی `D:\Amir\...\Plan` دسترسی دارد و مستقل فایل تولید کرده — چندین بار محتوای این دو چت با هم همپوشانی/تکرار پیدا کرده (چهار سند «claudecode-brief» ساخته شد که سه‌تایشان اسناد قدیمی همین چت را جایگزین کردند). **قبل از ساختن هر سند تازه‌ای درباره‌ی رابط کاربری قراردادها، حتماً اول با `Filesystem:list_directory` و `Filesystem:read_text_file` چک کن که آیا آن چت قبلاً همین کار را نکرده باشد.**

## ۴. ساختار کامل پوشه‌ها (زیر `D:\Amir\Business\SUR\NewSur\Plan`)

```
contracts/                      ← ۸ قرارداد Solidity، کامنت انگلیسی (منبع صحت برای دیپلوی)
  genesis-seed-helpers/          ← ۳ قرارداد کمکی موقت (فقط برای شبیه‌سازی محلی)
contracts-fa/                   ← همان ۱۱ فایل، فقط با کامنت فارسی (برای خواندن انسانی)
  genesis-seed-helpers/
technical-design/                ← اسناد طراحی فنی، یادداشت دیپلوی، مرجع قراردادها
offchain-services/                ← اسپک سرویس‌های آف‌چین + بریف‌های رابط کاربری
governance/                       ← ساختار سازمانی
economics/                        ← توکنومیکس
business/                          ← سپیدنامه، مزیت رقابتی، نقشه‌ی بازاریابی
diagrams/                          ← نمودارهای SVG
presentations/                     ← پاورپوینت هیأت‌مدیره + تیم فنی (باینری — فقط از چت قابل‌دانلود)
brand/                              ← لوگو (svg + png + jpg، دوتای آخر باینری)
_archive/                           ← اسناد منسوخ‌شده (نه حذف‌شده)
sur-master-open-items.md            ← فهرست جامع همه‌ی تصمیمات باز پروژه
```

## ۵. تصمیمات معماری کلیدی (باید همیشه رعایت شوند)

1. **شش قرارداد ساختاری genesis** (`FoundationDAO`, `BlockRewardDistributor`, `ValidatorsRegistry`, `ValidatorsBoard`, `ValidatorsTreasury`, `IdentityRegistry`) در آدرس‌های ثابت `0x1111...` تا `0x6666...` مستقیم در `alloc` بلاک genesis تزریق می‌شوند.
2. ✅ **هیچ‌کدام از این شش قرارداد `constructor` ندارند.** دلیل مستند: قراردادهای دارای constructor که این‌طور genesis می‌شوند، روی Blockscout قابل‌وریفای نیستند (GitHub issue #631). مقادیر ساده مستقیم در بدنه با `🔶 FILL_IN` مقداردهی می‌شوند؛ برای mapping/آرایه (اعضای اولیه، ولیدیتورهای اولیه)، از قراردادهای کمکی در `genesis-seed-helpers/` استفاده می‌شود (شبیه‌سازی روی Anvil، استخراج storage).
3. ✅ کامپایل باید همیشه **بدون `viaIR`** انجام شود (همان دلیل وریفای). `BlockRewardDistributor.distributeRewards` قبلاً به‌خاطر «Stack too deep» بازنویسی و به توابع کوچک‌تر تقسیم شد تا بدون `viaIR` کامپایل شود.
4. **`SurenSale.sol` تنها قرارداد غیر-genesis است** — constructor دارد و طبیعی دیپلوی می‌شود.
5. **سورن ارز بومی زنجیره است** (مثل ETH)، نه یک توکن ERC20.
6. **توزیع اولیه‌ی genesis سه‌ردیفی است** (تصمیم نسبتاً تازه):
   - ۲۰,۰۰۰,۰۰۰ سورن → `FoundationDAO` (توسعه/اکوسیستم)
   - 🔶 مبلغ هنوز نامشخص → جبران ولیدیتورهای شبکه‌ی قبلی (خسارت، نه هدیه)
   - ۱۵,۵۳۰,۰۰۰ سورن → تسویه‌ی بدهی بنیاد، **ردیفی** (نه یک‌جا) به آدرس شخصی هر طلبکار مستقیم در genesis — نه به آدرس مشترک هیأت‌مدیره (برای جلوگیری از تضاد منافع/self-dealing).
7. **حکمرانی دوشاخه:** بنیاد (۱۵ عضو، `FoundationDAO`) کاملاً مجزا و بدون کنترل بر شبکه/ولیدیتورها؛ ولیدیتورها با `ValidatorsRegistry` (رأی کامل، پارامترهای امنیتی) + `ValidatorsBoard` (۵ عضو، رأی‌گیری تأییدی مستمر، فقط پارامترهای اقتصادی ورود و چرخش اوراکل) + `ValidatorsTreasury` (خزانه‌ی مشترک ولیدیتورها — بودجه‌ی ترویج خودگردان با انگیزه‌ی اقتصادی، نه پورسانت شخصی).
8. **چهار اوراکل عملیاتی مجزا**، هرکدام کلید جدا در HashiCorp Vault: `verifier` (لایوینس، چرخش با هیأت)، `distributionOracle` (RewardRouter، چرخش با هیأت)، `identityOracle` (احراز هویت، چرخش با **بنیاد**)، `paymentOracle` (فروش سورن، چرخش با بنیاد اکثریت ساده).
9. **مکانیزم رزرو موجودی در `SurenSale`:** قفل ۱۵دقیقه‌ای قبل از هدایت به درگاه پرداخت، جلوگیری از فروش مضاعف، مهلت انقضای grace برای تأییدیه‌ی دیرهنگام، محدودیت یک رزرو فعال به‌ازای هر کاربر — کامل در `sur-suren-sale-spec.md`.

## ۶. وضعیت فعلی نرم‌افزارهای جانبی (خلاصه‌ی `sur-software-inventory.md`)

| # | نرم‌افزار | سند مرجع فعلی |
|---|---|---|
| ۱ | RewardRouter | `sur-reward-router-spec.md` |
| ۲ | ابزار genesis.json | `sur-genesis-builder-tool-spec.md` |
| ۳ | سرویس Verifier | `sur-verifier-service-spec.md` |
| ۴ | اپ ولیدیتور | `sur-validator-app-claudecode-brief.md` |
| ۵ | بلاک اکسپلورر | Blockscout (فقط پیکربندی) |
| ۶ | داشبورد حکمرانی | `sur-governance-dashboard-claudecode-brief.md` |
| ۷ | پرتال فروش سورن | `sur-suren-sale-portal-claudecode-brief.md` |
| ۸ | قرارداد+سرویس SurenSale | `sur-suren-sale-spec.md` + `contracts/SurenSale.sol` |
| ۹ | قرارداد+سرویس IdentityRegistry | `sur-identity-registry-spec.md` + `contracts/IdentityRegistry.sol` |
| ۱۰ | اپ احراز هویت عمومی | `sur-identity-app-claudecode-brief.md` |

⚠️ اسناد `sur-verifier-app-spec.md`, `sur-governance-dashboard-spec.md`, `sur-sale-dashboard-spec.md`, `sur-identity-app-product-plan.md` **منسوخ شده‌اند** (در `_archive/`) — جایگزین‌های بالا کامل‌تر هستند، به آن‌ها ارجاع نده.

## ۷. مهم‌ترین موارد باز (فهرست کامل در `sur-master-open-items.md`)

- پارامترهای امنیتی `ValidatorsRegistry` (`slashBps`, `exitCooldown`, و ۶ مورد دیگر) — هنوز عدد نگرفته‌اند (`🔶 FILL_IN` در کد).
- مبلغ دقیق «جبران ولیدیتورهای شبکه‌ی قبلی».
- انتخاب سرویس‌دهنده‌های خارجی: eKYC، پیامک/تلگرام OTP، درگاه پرداخت.
- اجرای واقعی ابزار genesis (فقط اسپک آماده است، کد نوشته نشده).
- محتوای خالی سپیدنامه (پیشینه، تیم، نقشه‌ی راه، حقوقی).

## ۸. سبک کاری مورد انتظار (آموخته‌شده از این گفتگو)

- کامنت کد Solidity: **همیشه انگلیسی** در `contracts/`؛ نسخه‌ی موازی فارسی در `contracts-fa/`.
- قبل از هر ادعای فنی مبهم (مثل رفتار Blockscout، تنظیمات کامپایلر)، با `solc` واقعی یا جستجوی وب تأیید کن، حدس نزن — چند اشتباه (مثل ادعای اولیه‌ی نادرست درباره‌ی انگیزه‌ی اقتصادی gasPrice) در این گفتگو همین‌طور اصلاح شد.
- وقتی دو سند به‌نظر یک چیز را توصیف می‌کنند، قبل از ادغام، **کامل هر دو را بخوان** و مطمئن شو یکی frontend/دیگری backend نیست.
- بعد از هر تغییر، فایل‌های مرتبط (اسناد ارجاع‌دهنده، zip نهایی در sandbox، نسخه‌ی هارد لوکال) را هماهنگ نگه دار — این پروژه چندین‌بار دچار ناهماهنگی نسخه بین sandbox و هارد لوکال شده.
